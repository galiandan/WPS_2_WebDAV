[project]
name = "wps-enterprise-adapter"
version = "0.2.0"
description = "Experiment-first adapter for an authorized WPS enterprise cloud drive account"
requires-python = ">=3.11"
dependencies = []

[tool.wps-adapter]
phase = "prototype"
network_calls_enabled = true
