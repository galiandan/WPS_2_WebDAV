# Changelog

本项目遵循 Keep a Changelog 风格。版本号用于记录适配器行为变化，不代表 WPS 官方兼容性承诺。

## [Unreleased]

## [1.6.0] - 2026-09-22

### Added

- 离线下载：公网 HTTP(S) 文件经 VPS 下载后上传到当前 WPS 目录，同名不覆盖；支持服务端持续执行、分阶段字节进度和受控取消/重试。
- 后台 ZIP：所选文件和目录在服务器生成完整 ZIP，任务中心提供下载和到期时间，结果保留 1 小时。
- 传输任务按账号、权限策略、实际空间和目标身份绑定；私有持久化记录支持重启后查看中断状态，不自动重放。
- 公网地址与每次重定向均验证 DNS 并固定连接 IP，不转发 Cookie、Authorization 或 Referer；源 URL 仅在 0600 私有任务记录中保留供重试，不进入公共状态、服务日志或浏览器存储。

### Fixed

- 修正源码构建和安装器源码回退的默认版本号，避免未注入构建版本时仍显示旧的 1.0.9。

### Limits

- 离线下载与后台 ZIP 必须配置并登录适配器账号；离线下载还需要上传权限。传输队列最多 16 个排队/执行任务、单个工作线程和 100 条历史。
- 单任务最多 1 GiB、设 30 分钟执行预算（已进入 WPS 上传的在途请求可能在预算到期后继续，需核对结果），传输临时目录总量最多 2 GiB；共享上传/下载及磁盘预留预算，离线下载还受 `WPS_MAX_UPLOAD_BYTES` 限制。
- 进入 WPS 上传阶段后不允许取消或重试；失败、超时或重启后的未知结果需要先核对目标目录。后台 ZIP 过期后需重新打包；浏览器上传仍需保持页面打开。

## [1.5.0] - 2026-09-22

### Added

- 文件/目录只读分享：有效期、可选提取码、撤销和独立访客页面；链接密钥仅创建时显示，服务端只保存验证哈希。
- 分享授权绑定创建者、目标和实际空间；权限变化、替换、撤销或过期后失效。访客接口与普通 REST/WebDAV/管理功能隔离。
- ZIP 内容浏览与单文件下载：有界 Range 读取，不解压到服务器目录，支持 UTF-8、Store/Deflate 和受限 ZIP64。
- 分享、ZIP 的密码/撤销/目录边界、资源预算、取消、CRC 及浏览器回归测试。

### Limits

- 分享默认 7 天、最长 30 天；访客授权最长 1 小时，重启后需重新打开原链接。分享不提供写入或管理能力。
- ZIP 浏览拒绝加密、多卷、链接、危险/冲突路径和不支持的压缩方法；单条目解压最多 128 MiB，并限制压缩比、读取量和时长。

## [1.4.0] - 2026-09-22

### Added

- 新增成员账号管理：独立目录、读取/上传/删除权限，统一覆盖网页、REST 与 WebDAV；安装管理员保持原有配置方式。
- 成员密码使用随机盐 PBKDF2 验证值，私有持久化；每个账号拥有独立 2FA、恢复码和 Passkey。
- 搜索、任务历史、编辑版本与前端草稿按账号/策略隔离；用户变更后旧会话、缓存及排队操作重新验证。
- 目录授权绑定实际 WPS 空间和接口环境，防止不同空间同为根 ID `0` 时误跟随同名映射。

### Fixed

- REST 与 WebDAV 的成员相对路径映射到同一实际路径检查锁；只读或仅上传用户不能通过其他协议越过写入限制。
- Basic 专用凭据导入路由同时校验多余斜杠别名，避免管理员网页 Cookie 绕过该接口的 Basic 要求。
- 登录过期后清空页面中的旧账号内容；未保存编辑草稿仅在同一账号和策略重新登录时于当前页面恢复。

## [1.3.0] - 2026-09-22

### Added

- 音视频播放器：支持浏览器可播放格式、同目录播放列表、倍速、播放进度记忆，以及本地 VTT/SRT 字幕；关闭或切换文件释放播放资源。
- 网格图片缩略图：按可见区域加载 JPEG/PNG/GIF 首帧，受输入大小、像素数、并发和缓存预算限制；失败保留文件图标。
- Markdown 渲染、源码高亮和原文切换；目录 README 自动显示说明，编码切换与文本编辑保存后更新渲染。
- 富文本依赖随 Go 二进制内嵌，不依赖 CDN 或 Node；保留上游许可证、版本及完整性记录。

### Limits

- 不提供音视频转码；字幕留在当前浏览器。富文本在可中止的后台线程中处理，超限或复杂内容回退原文。
- 原始 HTML 不执行，远程图片不自动加载；内部相对路径经过规范化。源码预览扩展不改变在线编辑允许的扩展名。

## [1.2.0] - 2026-09-21

### Added

- 新增后台任务中心：批量复制/移动/删除转为服务端任务，关闭网页后继续；显示逐项进度、错误与历史，支持取消后续项目和受控重试。
- 任务记录以 0600 JSON 原子保存；重启后标记中断，不自动重放。旧任务绑定账号、工作区和源/目标身份，防止同名文件被替换后误执行。
- 新增在线文本编辑：完整读取最多 2 MiB，支持中文编码切换、UTF-8 保存和 Ctrl/Cmd+S；冲突、锁定或失败保留当前页面草稿。
- 编辑保存前重新读取校验版本，保存后复读确认；不确定写入结果会明确提示核对，不自动重试。

### Limits

- 任务使用单个工作线程，最多 16 个排队/执行任务、每任务 100 项、最近 100 条历史；进度按所选项目显示。取消不会回滚已完成项目，当前项目可能继续完成。
- 浏览器上传仍需保持页面打开；任务中心单独显示其状态。文本编辑的草稿不跨页面刷新保存；WPS 接口没有原子条件覆盖，外部写入仍可能在检查与上传之间竞争。

## [1.1.0] - 2026-09-21

### Added

- 图片/PDF 在线预览：图片缩放、同目录切图，PDF 使用浏览器内置阅读器；复用认证、下载并发与 Range 流式传输。
- 多选与批量复制/移动/删除：支持复选框、Shift 连选、筛选全选和逐项结果；同名不覆盖，保留失败项目供处理。
- 文件夹上传：选择或拖入目录并保留结构，支持拖放空目录、分页枚举、已有目录合并确认、取消与失败重试。
- 多选文件/目录流式 ZIP 下载：保留空目录，限制遍历深度、数量和总大小；中途失败直接中止下载，不生成貌似成功的残缺 ZIP。
- 全局文件名/路径搜索：按空间建立有界目录索引，支持类型筛选、分页、结果预览和目录定位；明确扫描进度、范围、更新时间和不完整原因。
- GitHub CI 新增全部离线浏览器回归和截图产物；功能实现进度见 docs/feature-roadmap.md。

### Fixed

- WebDAV 锁映射到网页使用的同一存储路径；批量和普通网页写入遵守已持有的锁。每个 WebDAV 请求固定根映射，避免设置切换使锁检查与实际操作指向不同目录。
- 搜索索引在 REST/WebDAV 写入、凭据或工作区热替换后失效；手动刷新清理目录缓存，旧扫描不能回填失效结果。
- 取消递归复制后不继续创建后续条目；修复旧上传队列定时器隐藏新队列及提示遮挡队列按钮的问题。

### Limits

- 批量操作每次最多 100 项，复制/移动沿用已有同空间限制。ZIP 最多 10000 项、64 层和 10 GiB 数据，不支持断点续传。
- 搜索索引保存在内存且只查询元数据；最多 50000 项、遍历 5000 个目录、64 层、32 MiB 元数据和约 10 分钟，外部 WPS 变更需手动刷新。

## [1.0.34] - 2026-09-21

### Fixed

- 修复 Docker 自定义部署目录下 Passkey 创建成功但网站保存失败：网页与登录安全设置默认跟随工作区文件目录，支持 `WPS_WEB_SETTINGS_FILE` 显式指定旧位置。
- 注册前检查安全设置能否持久化；保存失败返回明确提示，失败回滚后保留有效注册挑战以便重试。
- 正确解析带认证器扩展的 Passkey 注册数据，并按 WebAuthn 标准验证 ECDSA DER 登录签名；新增持久化、重载、登录和失败回归测试。

## [1.0.33] - 2026-09-21

### Changed

- 登录页移除蓝色背景与渐变装饰，改用主题中性背景；自动填充的用户名和密码输入框保持与表单一致的底色和文字颜色，兼容浅色与深色主题。

## [1.0.32] - 2026-09-21

### Changed

- 桌面目录栏改为左侧独立悬浮面板：距左边缘 24px，上下留白，使用圆角、细边框和轻阴影；文件区域同步预留间距。

## [1.0.31] - 2026-09-21

### Changed

- 桌面目录侧栏固定贴齐页面左边缘，移除悬浮卡片圆角与阴影；文件区域在侧栏右侧留出间距，手机端继续使用目录抽屉。

## [1.0.30] - 2026-09-21

### Added

- 左侧空间导航升级为按需加载的多级目录树：箭头展开/收起，点击名称进入目录，精确高亮当前位置，并自动展开深层路径；支持键盘左右键与手机抽屉导航。
- 目录树复用现有目录缓存和请求合并，新建、重命名、移动、删除及刷新后同步受影响目录；空目录和读取失败提供明确提示及重试入口，迟到响应不会恢复已移除的目录。
- 桌面目录侧栏加宽并独立滚动，长目录名省略显示、悬停可查看完整名称。

## [1.0.29] - 2026-09-21

### Added

- 文件工具栏新增“新建文本文件”：填写名称和内容即可在目录中创建 TXT、Markdown、JSON 等可预览格式，以 UTF-8 保存，支持空文件，创建后刷新列表并可双击预览。
- 新建窗口会保留失败或关闭后的当前页面草稿及原目标目录；保存期间防止重复提交，同名项目提示改名，不自动覆盖。

## [1.0.28] - 2026-09-21

### Changed

- 列表与网格视图支持双击可预览文件的名称、图标或空白区域直接打开在线浏览；单击仍选中文件，菜单入口继续保留。

## [1.0.27] - 2026-09-21

### Added

- 在线浏览扩展至 TXT、LOG、Markdown、CSV、JSON、XML、YAML、INI、CONF、TOML；新增编码切换、自动换行、字号调整和全屏阅读。
- 中文文本支持 BOM 识别、UTF-8 检查和 GB18030 回退，并可手动选择 GBK/GB18030、Big5、UTF-16 LE/BE；切换编码无需重复下载。

### Fixed

- 预览按原始字节传输，避免非 UTF-8 文档乱码；大文件仍限制为前 2 MiB，正确处理截断的多字节字符并显示实际限制。
- 关闭或切换预览会取消旧请求并中断上游阻塞读取；新增空文件、疑似二进制内容和解码异常提示，所有文档均按纯文本安全显示。

## [1.0.26] - 2026-09-21

### Fixed

- 页面首次打开时先显示加载提示，等待登录状态检查完成；已有有效会话或未启用网页认证时直接进入文件页，避免登录界面短暂闪现。未登录或检查失败时正常显示登录界面及错误提示。

## [1.0.25] - 2026-09-21

### Changed

- 所有挂载空间与基础客户端共享凭据刷新协调器，合并并发 401 的刷新请求；迟到的旧凭据 401 直接复用已轮换的会话，等待刷新可独立取消，刷新失败后仍可重新尝试。
- 挂载变化仅重建发生变化的空间，保留未变更空间的 Storage 和目录缓存；空间名称判重改为线性扫描。

### Fixed

- 串行处理挂载快照读取与路由重建，防止较早的重建结果覆盖较新的挂载配置；发布的挂载列表不再引用来源切片。
- 分页游标按字符串内容而非指针地址判重，遇到相同 offset 下循环切换的 next_filter 时及时停止，避免持续请求到分页上限。
- 下载取消会中断下载 URL 解析、对象请求和已打开流的阻塞读取，及时释放下载槽；取消不再被控制响应读取错误误报为无效上游响应。

## [1.0.24] - 2026-09-21

### Changed

- 后端目录缓存采用带索引的最小堆，将满容量淘汰从 O(n) 全表扫描改为 O(log n)，保留原有 TTL、同到期时间的插入顺序及同目录请求合并行为。
- 借鉴 OpenList 的 singleflight 请求身份与 Forget 思路，以在途请求身份替代永久累积的目录失效计数，避免长期写操作持续增加缓存辅助元数据；设计依据与基准记录见 docs/backend-cache.md。

### Fixed

- 修复全量缓存失效和单目录失效并发执行时读取 generation 的数据竞争。
- 阻止失效前取得、失效后才开始加载的旧 generation 键回填缓存；确保旧请求完成时不会删除或覆盖替代请求。

## [1.0.23] - 2026-09-21

### Changed

- 删除页面底部的“OpenList UI /”文字，将“管理”改为设置图标，点击仍打开设置；上游许可证和来源保留在源码中。
- 新增仓库贡献指南，明确每完成一个改动都必须发布新版本，并验证 CI、发布说明和安装包。

## [1.0.22] - 2026-09-21

### Changed

- 文件浏览器移植 OpenList-Frontend 的居中容器、三列列表、图标网格、顶部导航和右下角工具栏布局，继续调用现有 WPS REST 接口；上游原始组件和 MIT 许可证保留在 go/web/upstream/openlist。
- 支持点击文件行空白处打开、右键文件菜单和 Ctrl/Cmd+K 搜索；手机端默认收起悬浮工具栏，并显示触屏文件操作入口。

## [1.0.21] - 2026-09-20

### Fixed

- 上传队列运行期间禁用失败／跳过文件的重试，结束后恢复，避免点击后卡在“等待上传”；重试也遵守其他写操作的忙碌状态。
- 部分上传成功而其他文件失败或取消时，也刷新当前目录，及时显示已上传的文件。
- 覆盖确认返回后重新检查取消状态，避免已取消的任务继续发送上传请求；每次上传结束后清理当前请求引用。
- 目录加载期间暂停上传、新建和拖放上传入口，避免沿用上一目录列表判断重名；加载结束后恢复操作。
- 地址栏路径统一编码，修复目录名含百分号、字面量 `%2F` 等字符时刷新或前进后退打开错误路径的问题；移除可能吞掉下一次历史导航的 hash 抑制标记。

## [1.0.20] - 2026-09-20

### Fixed

- “WPS 已连接”状态浮层改为紧贴触发按钮下方 8px、与按钮右边缘对齐，修复宽屏时浮层固定在窗口右侧而远离按钮的问题。窗口缩放和页面滚动时同步定位，小屏保持边距，矮屏内滚动显示内容。

## [1.0.19] - 2026-09-20

### Fixed

- 空间切换的选中高亮立即跟随点击，加载期间显示旋转图标；空间列表未变化时保留原按钮，避免焦点丢失和导航重建。
- 打开尚未缓存的空间时立即隐藏旧目录和旧空状态，只显示当前目录的加载占位，避免新路径下仍停留着旧文件。
- 空间与目录切换正常复用 30 秒内的有效缓存，不再每次强制重新请求；过期缓存继续后台刷新，手动刷新和文件变更仍获取最新结果。缓存命中不重复重建文件列表，也不被当作上游连接恢复的依据。
- 桌面空间导航不再把焦点转移给隐藏的移动端菜单按钮，加载失败后正确结束空间的加载指示。

## [1.0.18] - 2026-09-20

### Changed

- 登录页按 OpenList 实际登录界面重做：浅蓝／深蓝背景、两角蓝色曲线、364px 居中表单、并排品牌标题、简洁输入框及清空／登录按钮；登录方式与主题切换移至底部，移除原有宣传文案和顶部切换条。背景 SVG 使用 OpenList-Frontend 的 MIT 授权实现，许可证随页面及源码保留。
- 登录页可直接切换浅色、深色或跟随系统，并在下次打开时恢复选择；保留密码、两步验证和 Passkey 流程。

## [1.0.17] - 2026-09-20

### Changed

- 网页改为参考 OpenList / OneList 的简洁文件浏览风格：居中布局、横向空间导航、灰白／深灰表面、蓝色操作及轻量文件图标，统一登录页、移动端和深色主题，移除登录页对外部字体与照片背景的依赖。
- 顶栏版本号改为紧凑的非模态更新浮层，取消居中大卡片和全屏遮罩；支持再次点击、点击外部、焦点离开与 Esc 收起，保留更新状态、更新日志及一键更新。

### Fixed

- 更新浮层中“重新检查”主按钮也强制刷新服务端版本缓存，避免刚发布的版本被旧检查结果遮蔽。

## [1.0.16] - 2026-09-14

### Changed

- 登录页整体换装为雾蓝夜色开屏（视觉复刻自 Sui 的 [InternalBeyond](https://github.com/Sui-IB/InternalBeyond) 开屏页，其界面设计为 CC BY-NC-SA 4.0，本项目为非商业使用并保留署名）：雾中花照片底图随二进制内嵌，整屏轻毛玻璃幕帘、左对齐衬线文字列（Cormorant Garamond 眉题 + Great Vibes 手写签名 + 宋体主句）、发光细线、玻璃质感分段控件与表单、错落淡入入场；登录成功后幕帘溶解露出底图再进入工作台。字体经 Google Fonts 加载，离线时按系统衬线/手写体回退；不支持 backdrop-filter 的浏览器以加重罩色兜底。登录页为固定夜色外观，不随亮暗主题切换；工作台视觉不受影响。

## [1.0.15] - 2026-09-13

### Fixed

- 网页更新对话框的"重新检查"按钮现在真正重新检查：`GET /api/v1/update` 新增 `force=1` 参数，携带时服务端跳过 10 分钟结果缓存直接请求 Release 源；打开版本对话框也走强制检查。后台启动探测与更新进行中的轮询仍使用缓存，不会增加对 GitHub 镜像的请求量。此前用户在发布后点"重新检查"，最长 10 分钟内仍会看到旧的"已是最新版本"结果。

## [1.0.14] - 2026-09-13

### Changed

- 内置网页整体视觉升级为"极光工作台"设计语言（参考 dub / shadcn/ui / AList / Tabler 等高 star 开源项目的设计体系，全部自研 CSS 实现，仍无任何外部前端依赖）：新增靛蓝→紫罗兰渐变主操作与品牌标记、页面底层极光氛围与登录页圆点网格、玻璃质感页头/登录卡/浮层（不支持 backdrop-filter 时自动回退实色）、彩色渐变文件类型图标砖、卡片化文件面板与胶囊面包屑、渐变辉光主按钮、流光骨架屏，以及配套的悬停上浮、菜单入场、行错峰等微交互。浅色与深色两套主题、全部响应式断点、减少动态偏好与键盘焦点样式同步重制；所有交互契约（元素 ID、类名、接口）保持不变，`docs/fd.md` 设计文档已同步改版。

## [1.0.13] - 2026-09-13

### Fixed

- WebDAV 挂载根目录的 `DELETE` 请求现在被本地拒绝：DAV 根映射到一个真实的企业文件夹，存储层的"根不可删除"防护看不到这层映射，此前对 `/dav/` 的 DELETE 会把整个映射目录送进回收站。防护同时加在 DAV 调度层和 DAV 视图层。
- 网页一键更新的自动刷新改为"确认恢复后才重载"：此前看到"正在重启"后固定延时 2.2 秒重载，重载经常落进监听空窗，浏览器只会留下隧道/代理的错误页，自动刷新随之中断，用户只能手动刷新。现在更新期间轮询失败会持续探测 `/healthz`（每 1.5 秒，不限次数），只有在新进程开始应答（版本号变化或 API 报告更新已结束）时才重载一次；服务长时间未恢复时仅提示可手动刷新，探测继续。

- 网页一键更新完成后，页面在服务恢复时自动重载进入新版本（配合上一条：不再依赖固定延时，也不会再出现"更新检查失败"假报）。

## [1.0.12] - 2026-09-13

### Fixed

- 运行环境早于 spool 环境变量的容器部署，大文件上传不再必然 `507`：容器创建时快照的环境里没有 `WPS_UPLOAD_SPOOL_DIR`（安装器是后来才写入该变量的），镜像又可能没有 `/tmp`，超过内存阈值（默认 8 MiB）的上传全部失败。现在 spool 目录按「配置目录 → resume 目录 → 系统临时目录 → 可执行文件同目录」逐个探测可用性，第一个可写者胜出，选择结果与残余问题都在启动日志可见。

### Changed

- 目录列表默认每页 `200` 条（此前 `20`，对应 Python 参考实现的签名默认值）：冷目录打开的串行上游往返次数按页大小等比例下降，大目录首开从几十次请求降到个位数。上游 v5 列表接口对大 `count` 的接受度已实测验证，`next_offset` 分页保证行为始终正确。

- 上游瞬态失败重试加强：`429`（限流）纳入可重试状态，重试次数从 1 次增加到 2 次，间隔指数退避（250ms → 500ms），并遵守 `Retry-After` 响应头（封顶 5 秒）。仍然只对幂等 GET/HEAD 生效，任何写操作都不会被重放。

- 变更后缓存按目录精准失效（服务端与网页端）：此前任何上传、改名、移动、删除都会清空服务端全部目录缓存（容量 1024 个目录），网页端还在每上传一个文件后清空整个前端目录缓存，10 个文件的上传会触发 10 次全量冷重建。现在只失效受影响父目录的列表，其余目录继续命中缓存；并发在途的列目录请求仍被按目录的 epoch 校验拦下，不会把变更前的旧数据写回缓存。

- PROPFIND 与 REST 列表不再对同一路径做第二次全树解析：元数据阶段解析得到的条目直接用于列出子节点（WebDAV 遍历的每一层都按 ID 下行）。此前当首次解析耗时超过 2 秒的目录缓存 TTL 时，同一次请求会把整条根到目标的解析链再走一遍，在上游本已缓慢时恰好把上游请求量翻倍。

- Workspace 状态文件的热加载检查节流：每次访问都会在全局锁下 `Lstat` 状态文件，而每次上游请求要多次读取 workspace（mode/group/root），一次目录列表因此串行执行 3-4 次加锁系统调用；文件一旦变化，所有并发请求还会排在一个完整的文件读取加解析后面。现在状态检查有 500ms 下限，外部登录脚本的改动最迟在窗口结束后被看到，适配器自身的写入仍直接更新内存与 mtime。

- 内置网页：变更后按受影响目录精准清理前端缓存（上传改为每个文件清理其目标目录，而不是每文件清空整棵缓存树）；目录搜索输入加 150ms 防抖，大目录里打字不再每个按键都全量重建列表 DOM；状态轮询从 30 秒调整到 25 秒，不再与服务器 30 秒探测 TTL 赛跑——此前每次轮询都恰好落在 TTL 过期后，变成每个打开的标签页两次真实上游请求。

- 热路径杂项：无 body 控制请求的排空缓冲改用 `sync.Pool` 复用（此前每个 PROPFIND/DELETE/建目录请求都分配 64 KiB）；大上传的磁盘空间探测从每 1 MiB 一次降为每 64 MiB 一次（1 GiB 上传从约 1000 次 statfs 降到 16 次）；列表条目不再保留从未被任何代码读取的上游原始 JSON——目录缓存最多持有 1024 个目录的条目，这份死数据会让缓存内存成倍增长。

## [1.0.11] - 2026-09-13

### Fixed

- 网页内一键更新不再误报失败：服务自重启会短暂切断监听连接，落在重启空窗内的状态轮询此前被判成"更新检查失败"并停止轮询，页面停留在失败提示直到手动刷新。现在更新进行期间的轮询失败被视为预期情况，页面保持"正在更新/正在重启"状态并持续重试，新服务应答后自动刷新进入新版本。

## [1.0.10] - 2026-09-13

### Fixed

- Docker 安装器把上传 spool 目录指向 bind 挂载的 `data/uploads`：容器镜像不保证存在 `/tmp`，此前超过 8 MiB 的上传在预留 spool 时会返回 `507`。
- 发布流水线把标签版本号注入预编译二进制，网页内更新流程可以正确读取和比较版本。
- 兼容 Cloudflare Tunnel 的 chunked 重帧请求：隧道把 HTTP/2 上无 Content-Length 的请求（网页端"新建文件夹"的空 POST、客户端的无 body PROPFIND 等）转发到源站时改用 chunked 编码，适配器此前只豁免 MKCOL，其余一律 `400`，导致通过隧道创建目录始终失败。现在所有方法的 chunked 请求都进入正常处理（解码后的 body 由有界排空消费），上传仍要求显式 Content-Length。

### Changed

- 内置网页的页面、样式和脚本资源按客户端 `Accept-Encoding` 提供预计算的 gzip 变体：首次加载传输量从约 204 KB 降到约 46 KB，ETag 仍指向未压缩实体，304 重验证行为不变；未声明 gzip 的客户端以及全部 WebDAV/REST 请求路径仍收到原样字节，外部客户端兼容性与合同字节对齐不受影响。
- WPS 上游连接池为每个主机保留最多 16 个空闲连接（此前为 Go 默认的 2 个）：网页端并行刷新状态与目录内容、多空间并发请求不再反复重建 TLS 连接。
- 上传 spool 目录启动自检：未配置 `WPS_UPLOAD_SPOOL_DIR` 时自动回退到 resume 目录（容器部署始终挂载的数据路径），目录缺失会自动创建；仍不可用时在启动日志输出警告，提示超过内存阈值（默认 8 MiB）的上传将返回 `507`——容器镜像缺少 `/tmp` 导致大文件上传全部失败的问题从此在部署层面不再可复现。
- 内置网页的页面、样式和脚本资源从 `no-store` 改为 `no-cache` + 内容 ETag 协商缓存：浏览器本地保留资源副本，每次访问先向服务器校验，未变化时仅传输响应头（304），内容更新后才重新下载。升级服务不再重复传输约 200 KB 的静态资源，同时由于每次使用前都会与当前二进制比对校验值，仍然不会出现升级后新旧资源混用的问题。

## [1.0.9] - 2026-09-11

### Fixed

- 兼容部分 WebDAV 客户端用 chunked 编码发送空 `MKCOL` 请求，避免通过 Cloudflare Tunnel 创建同步目录时返回 `400`。

## [1.0.8] - 2026-09-11

### Changed

- 更新入口改为名称下方的版本号胶囊：有新版本时变黄，点击可查看详情、打开发布页或执行更新。
- 更新弹窗提供手动重新检查和明确的失败状态，不再依赖首页横幅。

## [1.0.7] - 2026-09-11

### Fixed

- 增加始终可见的更新检查入口，更新检查失败时不再让用户看不到任何操作位置。
- 修正默认国内更新源，避免 `ghfast.top` 返回 `403 Invalid input` 导致更新提示消失。

## [1.0.6] - 2026-09-11

### Fixed

- 目录后台刷新不再重播整组文件的入场动画，避免缓存列表刷新后突然跳动。
- 移除会改变内容区高度的刷新横线和“正在刷新当前目录”提示，刷新状态改由工具栏图标表示。
- 目录导航不再等待独立的 WPS 状态探测，也不会因一次状态探测抖动隐藏已成功加载的目录。
- WPS 只读 GET 请求遇到短暂网络断开或 408、502、503、504 时最多自动重试一次；写请求不会被自动重放。

## [1.0.5] - 2026-09-11

### Added

- 网页首页支持检查 GitHub Release，并可一键下载当前架构的预编译二进制、原子替换运行文件和自动重启。

### Changed

- 目录切换优先绘制浏览器已有缓存，再并行刷新 WPS 状态和目录内容，减少重复点击时的等待感。
- Docker 服务从部署目录中的运行文件启动，为一键更新后的容器重启保留新版本。

## [1.0.2] - 2026-09-11

### Fixed

- 企业 WPS Cookie 在账号类型探测后短暂轮换时，保留第一次已经验证成功的凭据快照。

## [1.0.1] - 2026-09-11

### Fixed

- 修复企业 WPS 使用 `*.wps.cn` 入口或非 `csrf` 命名的官方 CSRF Cookie 时无法登录的问题。

## [1.0.0] - 2026-09-10

### Added

- 重做网页工作台视觉系统：登录、空间导航、文件区、状态、上传队列和弹层统一为正式版界面。

### Changed

- 登录页改为聚焦式单栏布局，工作台改为稳定的空间导航和文件画布结构。
- 统一浅色、深色主题的颜色、边界、间距、响应式行为和触摸目标。

## [0.9.108] - 2026-09-10

### Fixed

- 修复企业 WPS 登录时 CSRF Cookie 位于受信任的其它 `kdocs.cn` 子域而被忽略的问题。

## [0.9.107] - 2026-09-10

### Fixed

- 登录助手显式启用终端 Readline，VPS 地址、端口和连接信息输入现在支持方向键、退格、删除和 Home/End 编辑。

## [0.9.106] - 2026-09-10

### Fixed

- 修复个人 WPS 删除请求缺少空间和 CSRF 字段导致的 403，个人空间文件删除现在使用与 WPS 接口一致的请求体。

## [0.9.105] - 2026-09-10

### Added

- 网页文件菜单支持 `.txt` 在线浏览，服务端以纯文本流返回并限制预览大小，超大文本会明确提示截断并保留完整下载入口。

## [0.9.104] - 2026-09-10

### Changed

- 产品默认名称统一为 `WPS Drive`，网页、WebDAV 根目录显示名、Passkey/TOTP 和安装文档不再使用旧产品名称。
- 升级时自动将恰好等于旧默认名称的持久化设置迁移为 `WPS Drive`，用户自定义的其它名称保持不变。

## [0.9.103] - 2026-09-10

### Added

- 支持个人 WPS 网盘的自动识别、空间发现、动态接口切换和个人下载路由。

### Fixed

- 登录页两侧品牌与标题统一顶部基线，密码登录和 Passkey 登录切换时不再产生位置跳动。

## [0.9.102] - 2026-09-09

### Fixed

- 固定登录卡片的垂直布局基准，切换密码登录和 Passkey 登录时品牌与标题位置不再跳动。

## [0.9.101] - 2026-09-09

### Fixed

- 固定密码登录和 Passkey 登录面板的内容高度，切换方式时外框不再跳动或变小。

## [0.9.100] - 2026-09-09

### Changed

- 登录方式选择器改为带滑动指示器的切换控件，密码登录和 Passkey 登录之间的切换更直观。
- 登录面板切换增加轻量进入动画，并保留减少动态效果设置的兼容性。

## [0.9.99] - 2026-09-09

### Fixed

- 补充网页登录页本地服务状态点样式，确保状态提示在浅色和深色主题下都清晰可见。

## [0.9.98] - 2026-09-09

### Changed

- 重做网页登录界面，密码登录和 Passkey 登录改为清晰的双模式面板。
- 登录页适配桌面双栏和移动端单栏布局，兼容浅色、深色和系统主题。
- 密码、两步验证码和 Passkey 登录增加明确的加载状态、焦点反馈和无障碍状态标记。

## [0.9.97] - 2026-09-09

### Fixed

- 修复网页文件操作菜单层级错误：菜单使用不透明背景，打开时不会被其他文件行的操作按钮覆盖。

## [0.9.96] - 2026-09-09

### Changed

- 网页可同时挂载多个已选 WPS 空间，例如 A 和 B；每个网页空间从自己的 WPS 根目录开始浏览。
- WebDAV 改为独立的单根子树视图，例如 `/dav/` 映射到 A 的 `/web`，不会访问 B 或 A/web 之外的目录。
- 登录器先选择网页空间，再单独选择一个 WebDAV 根目录所属空间和文件夹；目录选择可以跳过并在网页设置中完成。
- 网页设置接口将 `locations`（网页空间列表）与 `current`（唯一 WebDAV 根目录）分离。

## [0.9.95] - 2026-09-08

### Changed

- WebDAV 根目录改为单空间单目标：登录助手只能选择一个 WPS 空间和一个目录，上传、下载、移动和复制不再面对多个空间的歧义。
- 目录选择支持直接跳过；跳过后暂时使用所选空间根目录，登录完成后可在网页“设置 → WebDAV 存储位置”中选择具体目录。
- 删除文档和交互中的多空间虚拟根目录说明，统一网页、登录助手和 API 的单空间映射语义。

## [0.9.94] - 2026-09-08

### Added

- 网页登录支持可选 TOTP 两步验证、一次性恢复码和 Passkey/WebAuthn 登录。
- 网页安全设置支持启用/关闭 TOTP、添加/删除 Passkey；安全状态持久化到私密目录，服务重启后保持有效。
- Passkey 校验包含挑战、来源、RP ID、认证器签名和签名计数检查；实现不增加第三方运行时依赖。

### Security

- 恢复码只保存 SHA-256 哈希，使用后立即失效；Passkey 私钥不会离开浏览器或硬件设备。
- 2FA/Passkey 只作用于网页登录，不改变 WebDAV 客户端使用 Basic Auth 的兼容行为。

### Fixed

- 持久化失败时正确回滚 TOTP、恢复码和 Passkey 状态；2FA 失败次数现在会写回挑战并限制重试，未过期挑战数量也有上限。

## [Unreleased]

## [1.0.25] - 2026-09-21

### Changed

- 所有挂载空间与基础客户端共享凭据刷新协调器，合并并发 401 的刷新请求；迟到的旧凭据 401 直接复用已轮换的会话，等待刷新可独立取消，刷新失败后仍可重新尝试。
- 挂载变化仅重建发生变化的空间，保留未变更空间的 Storage 和目录缓存；空间名称判重改为线性扫描。

### Fixed

- 串行处理挂载快照读取与路由重建，防止较早的重建结果覆盖较新的挂载配置；发布的挂载列表不再引用来源切片。
- 分页游标按字符串内容而非指针地址判重，遇到相同 offset 下循环切换的 next_filter 时及时停止，避免持续请求到分页上限。
- 下载取消会中断下载 URL 解析、对象请求和已打开流的阻塞读取，及时释放下载槽；取消不再被控制响应读取错误误报为无效上游响应。

## [1.0.24] - 2026-09-21

### Changed

- 后端目录缓存采用带索引的最小堆，将满容量淘汰从 O(n) 全表扫描改为 O(log n)，保留原有 TTL、同到期时间的插入顺序及同目录请求合并行为。
- 借鉴 OpenList 的 singleflight 请求身份与 Forget 思路，以在途请求身份替代永久累积的目录失效计数，避免长期写操作持续增加缓存辅助元数据；设计依据与基准记录见 docs/backend-cache.md。

### Fixed

- 修复全量缓存失效和单目录失效并发执行时读取 generation 的数据竞争。
- 阻止失效前取得、失效后才开始加载的旧 generation 键回填缓存；确保旧请求完成时不会删除或覆盖替代请求。

### Changed

- 文件浏览器移植 OpenList-Frontend 的居中容器、三列列表、图标网格、顶部导航和右下角工具栏布局，继续调用现有 WPS REST 接口；上游原始组件和 MIT 许可证保留在 go/web/upstream/openlist。
- 支持点击文件行空白处打开、右键文件菜单和 Ctrl/Cmd+K 搜索；手机端默认收起悬浮工具栏，并显示触屏文件操作入口。

### Added

- 支持个人 WPS 网盘：登录助手自动识别个人账号、发现个人空间并保存 `mode: personal`；Go 服务按工作区状态使用个人接口，网页和 WebDAV 无需额外配置空间 ID。
- 登录助手优先按名称自动发现当前账号可见的企业空间；多个空间时只允许选择一个，发现失败会明确停止同步，并在同步前验证所选根目录。
- 新增 Linux 多架构预编译二进制 Release 工作流，Native 和 Docker 安装器优先下载预编译服务，失败后才回退源码现场编译。

### Changed

- 项目开源协议从 MIT 改为 GNU GPL-3.0-or-later。
- 安装器移除发布清单、Go 工具链和归档 SHA-256 校验；Native、Docker 都只使用一个明确下载地址，不再自动切换候选地址。
- 分片上传不再把 WPS 返回的 Content-MD5 与本地内容摘要进行比对，按 WPS 指令原样发送。

### Fixed

- 修复个人账号在服务启动后或登录导入后仍沿用企业接口的问题；个人列表、下载、移动和删除现在使用 OpenList WPS 驱动对应的个人路由。
- 修复个人登录器 Cookie 筛选残留企业变量和工作区验证写死企业路径的问题；个人 `wps.cn` Cookie 会在账号类型确认后重新筛选。
- 修复 Docker 安装器在 Ubuntu 等系统使用 legacy builder 并显示弃用警告的问题；现在会自动安装可用的 Buildx，并优先使用 `docker buildx build --load`。
- 修复卸载脚本在原生部署主机没有 Docker 命令时因 `--remove-image` 失败的问题；统一卸载流程现在自动识别 Native/Docker，并始终删除本机配置和凭据。
- 按 D-02 决策同步修正状态检查：根目录列表遇到 401 时不再触发 refresh-token 刷新，整个 status 路径保持只读（Python 与 Go 实现已一致）。
- 修复登录助手多空间选择时不显示空间名称和序号的问题。
- 移除文档、提示和测试中的特定账号空间名称；空间列表始终使用当前登录 WPS 账号返回的名称。
- 修复无名称工作区被错误显示为群组 ID 文件夹的问题；默认登录流程拿不到空间名称时不再写入不完整配置。
- 修复 WPS 目录分页未传递 `next_filter` 导致第二页重复条目、目录 502 的问题。
- 允许 WPS 分页边界自然重叠并跨页去重，避免合法重复条目被错误报告为 502。
- 修复下载正文已完成但浏览器仍等待的问题；下载响应现在显式刷新并关闭连接。
- 修复改名文件的 WebDAV `COPY` 误用 WPS 原生复制接口，避免返回一个云盘中并不存在的目标路径；只有保留源文件名时才使用原生复制。
- 安装器现在在解压前拒绝符号链接、硬链接、设备文件和 FIFO 等特殊归档条目，降低供应链归档被篡改时的风险。
- 修复下载响应使用过期 WPS 元数据大小导致客户端在进度显示 100% 后继续等待的问题；现在优先使用对象存储实际长度，未知长度时使用连接关闭完成响应，并在上游提前结束时正确释放连接。
- 强化下载完成信号：响应发送完最后一个字节后显式关闭 TCP 写方向，禁止中间代理改写内容，并为浏览器补充兼容性文件名。网页下载交给浏览器原生下载流程处理。
- 网页读取目录后后台预取最多 24 个直接子文件夹，使用 2 个并发请求和 30 秒元数据缓存；刷新及文件变更会自动清理缓存，减少进入常用子目录时的等待。
- 规范化独立登录助手的输入校验、浏览器关闭提示和超时错误；SSH 同步现在会完整保留多空间名称和选择配置。
- 安装器支持通过 `WPS_ADAPTER_BINARY_RELEASE_TAG` 和 `WPS_ADAPTER_BINARY_BASE_URL` 指定预编译 Release 来源，并保留无预编译资产时的 Go 构建回退。

### Documentation

- 重写 README 为安装、登录、多空间选择和访问的简化流程，统一当前可直接执行的命令与故障排查说明。
- README 中安装、登录和卸载命令统一使用国内 GitHub 加速地址，并明确说明不会自动切换下载节点。
- Go 服务已经成为 Native 和 Docker 的唯一生产运行时；Python 只保留为用户电脑上的登录助手。

## [0.9.92] - 2026-09-08

### Added

- 登录助手支持在关闭浏览器后按 WPS 空间选择具体文件夹作为 WebDAV 根目录。
- 网页设置支持浏览 WPS 空间并切换 WebDAV 存储位置，切换只更新映射，不移动远端文件。
- 工作区配置持久化保存目标文件夹 ID 和显示路径；网页、HTTP 导入和 SSH 同步行为保持一致。

## [0.9.91] - 2026-09-07

### Changed

- 删除不再参与当前项目的浏览器扩展、旧 Python 源码、旧工具和本地 WPS 抓包目录；生产服务仍只使用 Go 二进制，登录助手单独保留在项目根目录。
- Native、Docker、登录助手和文档版本统一升级到 `0.9.91`。

## [0.9.9] - 2026-09-07

### Added

- 提供 Linux 多架构预编译服务二进制；安装器默认先下载对应资产，失败后才回退源码现场编译。
- 新增发布工作流，自动构建 `amd64`、`arm64`、`386`、`armv6`、`armv7`、`ppc64le`、`riscv64` 和 `s390x` 资产。

## [0.9.8] - 2026-09-04

### Added

- 新增受 Basic Auth 保护的 `GET /api/v1/status`，使用 WPS `islogin` 只读预检和当前根目录最小列表验证。
- 状态预检支持成功缓存、失败退避和并发单飞，区分未配置、登录过期、工作区无权、上游不可用和异常响应。
- 网页初次加载和定时检查 WPS 状态，不再把适配器进程在线误报为 WPS 已连接。

## [0.9.7] - 2026-09-04

### Added

- 网页右上角新增云盘名称设置入口，名称可在网页内修改并持久化到服务端。
- 新增 `GET/PATCH /api/v1/settings`，修改后的名称对网页、WebDAV 和 REST 根目录元数据统一生效。
- 对自定义名称进行 HTML 和 JavaScript 安全编码，避免配置值破坏网页。

## [0.9.6] - 2026-09-04

### Added

- 网页文件管理器读取 `WPS_ROOT_NAME`，支持自定义浏览器标题、品牌名和根目录显示名称。
- 对自定义名称进行 HTML 和 JavaScript 安全编码，避免配置值破坏网页。

## [0.9.5] - 2026-09-04

### Fixed

- 修复卸载脚本只接受 `/usr/bin/python3`、导致旧版 `/usr/bin/python3.11` 服务无法卸载的问题；现在会校验完整服务标识并兼容 Python 3.x 路径。

## [0.9.4] - 2026-09-04

### Added

- 新增统一的 Native/Docker 卸载脚本，默认保留配置和凭据，支持显式清理与 Docker 镜像删除。
- 重写项目 README，集中说明部署、登录、访问、卸载、安全边界和当前限制。

## [0.9.3] - 2026-09-04

### Changed

- 登录助手默认使用 WPS 企业云盘根目录，不再把登录后自动恢复的旧文件夹误保存为适配器根目录。
- 新增 `--workspace-url`，只有明确指定具体 WPS 文件夹地址时才使用该文件夹作为适配器根目录。

## [0.9.2] - 2026-09-04

### Added

- 安装器下载优先使用国内 GitHub 加速节点，失败后自动回退到 GitHub 直连，并为每个地址设置连接和总超时。
- Native 和 Docker 安装器识别常见 Linux 包管理器；Native 在无 systemd 主机上使用便携后台模式，Docker 支持 systemd、OpenRC 和 SysV service 启动 daemon。
- Docker 安装器优先尝试国内 Python 基础镜像，并支持 `WPS_ADAPTER_DOCKER_BASE_IMAGE` 自定义镜像。
- 安装器归档路径校验和非 GNU `find`/`sha256sum` 兼容处理，保留阶段进度和下载进度显示。

## [0.9.1] - 2026-09-04

### Added

- Native 和 Docker 安装器显示阶段进度，并在下载项目归档时显示 curl 进度条。
- Docker 镜像构建阶段保留逐层构建输出，便于判断长时间任务是否仍在进行。

## [0.9.0] - 2026-09-04

### Added

- 登录助手从当前官方 WPS 企业云盘页面自动识别群组 ID 和映射根目录。
- Cookie、CSRF 和工作区状态通过 HTTP/HTTPS、SSH 或本地输出一起同步。
- 工作区状态以权限受限的 JSON 文件持久化，服务运行中可自动切换根目录。
- Native 和 Docker 安装器默认使用 `WPS_GROUP_ID=auto`、`WPS_ROOT_ID=auto`，不再询问群组 ID。

## [0.8.1] - 2026-09-03

### Security

- 安装器校验固定提交归档的文件清单和 SHA-256，拒绝归档中的符号链接和额外文件。
- 默认单次上传上限为 1 GiB，并在读取请求体前检查已声明的大小。
- Docker 以可写目录配合只读 Basic Auth 文件挂载，支持 Cookie/CSRF 的同目录原子轮换。
- REST 重命名/移动会同时检查源和目标锁；并发 WPS 会话刷新会串行执行。
- 带有跨站 `Origin`/`Referer` 的写请求必须指向当前适配器主机，并限制登录同步响应大小。
- HAR 脱敏会移除签名 URL、对象/租户/设备标识和文件名等敏感字段。
- 上游 JSON、对象存储控制响应和 multipart XML 响应增加内存上限。
- 拒绝异常 HTTP 请求分帧后继续复用连接，并限制登录助手的 CDP 连接只能访问本机回环地址。
- 递归 COPY 失败时尽力删除本次新建的目标目录。

## [0.8.0] - 2026-09-03

### Security

- 安装器默认从固定的完整 Git 提交归档安装，避免直接信任可变分支。
- 原生和 Docker 安装失败时尽量恢复旧应用、配置、凭据权限和服务状态。
- SSH 登录助手只能写入 `/etc/wps-adapter/secrets/` 下的直接文件。
- 签名对象存储地址限制在 WPS 域名，下载请求不跟随重定向。
- 控制请求体、目录分页、客户端连接和上传临时磁盘均增加边界保护。
- MOVE/COPY 不再为实现覆盖而先删除已有目标，避免失败时静默丢失数据。
- 手动配置的凭据文件拒绝符号链接、宽权限、非普通文件和过大文件。

## [0.7.0] - 2026-09-03

### Changed

- `wps_login.py` 现在是可独立下载运行的单文件登录助手，获取 Cookie 不再需要 clone 整个项目。
- HTTP/HTTPS 登录同步均可用；远程 HTTP 默认要求确认或 `--allow-http`。
- Native 和 Docker 安装器默认使用执行 `sudo` 的当前用户，并统一保护凭证文件权限。
- Docker 安装器从正确的 `deploy/Dockerfile` 构建，切换前完成构建，失败时尝试恢复原服务/容器。
- Docker 容器增加项目归属标记、当前用户 UID/GID 和能力限制，不再强制删除未知同名容器。

## [0.6.0] - 2026-09-03

### Added

- 新增 GitHub Raw 原生 systemd 一键安装脚本。
- 新增 GitHub Raw Docker 一键安装脚本、Dockerfile 和 Compose 示例。
- 原生、Docker 和 Python HTTPS 登录流程均支持自定义端口。
- Python SSH 登录流程支持自定义 SSH 端口。

### Security

- 安装器升级时保留 `/etc/wps-adapter/secrets/`，不会清空已有 Cookie、CSRF 或适配器密码。
- Docker 构建上下文排除 `.env`、secret、HAR 和抓包文件。

## [0.5.1] - 2026-09-03

### Added

- `wps_login.py` 无参数运行时会询问 VPS 地址和连接方式。
- SSH 登录支持密钥和密码认证；密码由系统 `ssh` 原生提示，不需要 `sshpass`。

## [0.5.0] - 2026-09-03

### Added

- Python 登录助手可自动检测 WPS 登录完成，不再要求回终端按回车。
- 新增受适配器 Basic Auth 保护的 HTTPS 凭据导入接口。
- 新增仓库根目录 `wps_login.py` 入口，可直接运行而不设置 `PYTHONPATH`。
- 新增交互式连接向导，支持 SSH 密钥、SSH 密码和 HTTPS；SSH 密码由系统 `ssh` 原生提示。
- SSH 同步和本地凭据输出继续保留为备用方式。

### Security

- 远程明文 HTTP 被登录助手拒绝；只有本机回环地址允许 HTTP 测试。
- HTTPS 导入不跟随重定向，Cookie 只放在请求体中，不进入 URL、日志或命令参数。
- 凭据文件更新尽量保持 Cookie 和 CSRF 成对，并使用权限受限的原子文件写入。

## [0.4.0] - 2026-09-03

### Added

- 本地隔离 Chrome 登录助手，可在官方 WPS 页面完成登录后通过 SSH 同步会话凭据。
- 基于 WPS SDK 观察结果的 `grant_token` 无交互续期和轮换 Cookie 持久化。
- WebDAV `COPY`、`LOCK`/`UNLOCK`、递归 `PROPFIND`、单范围下载和传输资源保护。
- 普通上传、覆盖更新和 10 MiB 分片上传流程。

### Security

- 登录助手不读取现有浏览器配置，不打印 Cookie 值，不把凭据放入命令参数。
- 原始抓包和本地 secret 文件保持在 Git 忽略范围内。

## [0.3.0] - 2026-09-03

- 增加 WPS 会话刷新原型和 Set-Cookie 持久化。

## [0.2.0] - 2026-09-03

- 增加 WebDAV 传输保护、Range、COPY 和锁兼容层。

## [0.1.0] - 2026-09-03

- 建立基于本人账号抓包验证的列表、上传、下载、目录和基础 WebDAV 原型。
- 多空间网页与单 WebDAV 子树映射：登录助手可选择多个网页空间，WebDAV 只映射其中一个空间的一个目录；网页设置中的空间列表和当前 WebDAV 位置分离。
